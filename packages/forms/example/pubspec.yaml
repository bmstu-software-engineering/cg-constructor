name: forms_example
description: Example app for the Forms package
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.3.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  forms:
    path: ../
  forms_annotations:
    path: ../packages/forms_annotations
  models_ns:
    path: ../../models_ns

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.3.0
  forms_generator:
    path: ../packages/forms_generator
  
flutter:
  uses-material-design: true
